lightgbm>=4.0,<4.6  # 4.7.x crashes on Windows (native access violation in Dataset.set_field)
scikit-learn>=1.3,<1.6
numpy>=1.24,<2
pandas>=2.0,<3
scipy>=1.10,<2
joblib>=1.3,<2
pyarrow>=14,<19
sentence-transformers>=2.2,<3
# onnxruntime and transformers are only needed if the §10.1 ARM latency gate fails
# and the OnnxEmbeddingBackend fallback path (features/embeddings.py) is used
# instead of sentence-transformers, or if USE_EMBEDDING_BRANCH is disabled and the
# TF-IDF-only path is chosen (in which case neither is required at all).
onnxruntime>=1.16,<2
transformers>=4.30,<5
pytest>=7,<9
